name: 'crypto_dbt'
version: '1.0.0'

profile: crypto_dbt

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  crypto_dbt:
    staging:
      +materialized: view
    silver:
      +materialized: incremental
    gold:
      +materialized: incremental